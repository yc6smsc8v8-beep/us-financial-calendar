US Economic + S&P500/Dow30 Earnings — Live ICS Feed (Render/Outlook-safe)

Deploy:
1) Push files to GitHub.
2) Render → New → Web Service → Environment: Docker → connect repo.
3) (Optional) Add env vars: FMP_API_KEY, LOOKAHEAD_DAYS, CACHE_TTL.
4) Deploy. Feed path: /us_financial_calendar.ics
